name: CI
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v1
    - uses: docker
    - run: docker build -t envygeeks.io
    - uses: docker://envygeeks/website:latest
    - run: script/build

/**
 * This should not use ES6 because
 * it gets imported into the gridsome.config.js
 * file to help configure critical stuff
 * so keep it simple stupid.
 */
module.exports = {
  baseFontSize: "100%",
  secondaryFont: "Noto Sans",
  layoutwidth: "750px",
  mainFont: "Lora"
}

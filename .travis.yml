script: script/test
group: deprecated-2017Q2
before_script: script/build
install: script/install
services: [docker]
sudo: required
os: linux
cache:
  bundler: true
  directories:
  - vendor/bundle
  - node_moduels
deploy:
- script: script/deploy
  provider: script
  on:
    branch: master
notifications:
  email:
    on_success: never
    on_failure: change

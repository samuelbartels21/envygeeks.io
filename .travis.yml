script: script/ci
services: ["docker"]
sudo: required
install: true
os: linux
cache:
  bundler: true
  directories:
    - vendor/bundle
jobs:
  include:
  - stage: precache
    script: script/install
  - script: script/test
    stage: test
